{% extends 'base.html.twig' %}

{% block title %}Gallerie index{% endblock %}

{% block body %}
    <h1>Gallerie index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for media in gallerie.getMedia %}
            <tr>
                <td>{{ gallerie.id }}</td>
                <td>{{ gallerie.nom }}</td>
                <td>
                    <a href="{{ path('app_product_show', {'id': gallerie.id}) }}">show</a>
                    <a href="{{ path('app_product_edit', {'id': gallerie.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_product_new') }}">Create new</a>
{% endblock %}
